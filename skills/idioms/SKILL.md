---
name: idioms
description: Use when language-specific implementation, interface, review, or refactor guidance is needed after the relevant language is known. Loads one applicable idioms pack rather than broad language guidance.
---

# Idioms

Select only the pack matching the language in the active scope. Read `references/idioms/<language>.md`; do not load unrelated packs.

Use the pack to challenge language-specific traps and select idiomatic local expression. Preserve repository conventions where they conflict with a generic recommendation unless the change explicitly includes a convention change.

If no matching pack is installed, state that and use the repository's established conventions and tooling.
