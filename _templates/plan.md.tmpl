# Plan — {{change-title}}

**Change ID:** {{id}}
**Architecture ref:** architecture.md
**Decisions ref:** decisions.md
**Date:** {{date}}

> This plan is the single source of truth for implementation. Each task is a checkbox. `implement` marks tasks complete in real time. Do not add tasks during implementation — if new work is discovered, kick back to `specify`.
>
> Every non-doc section has one machine-readable `Checkpoint unit` marker. Those IDs must exactly match all objects in `.changes/active/{{id}}/implementation-units.json`; each object requires `id`, exact `files`, `lockedTestFiles`, exact `baselineCommand`, and exact `finalCommand` matching the section fields below. File fields are JSON arrays inside code spans so locks cannot be reclassified by the declaration file.

## Traceability check

Every firm acceptance criterion must trace to at least one task. Every firm seam must have at least one firm-seam test task.

| AC ID | Task(s) | Firm-seam test task |
|---|---|---|
| AC-{{id}}-01 | Section N, Task M | Section N, Task K |

<!-- Fill before plan gate is approved. -->

---

## Section 1: {{section-name}}

> **Scope:** {{What this section covers. Files and modules touched. Decisions implemented.}}
> **Decisions implemented:** {{list of decision references from decisions.md}}
> **Seams touched:** {{list of seam IDs, with firmness}}
> **Checkpoint unit:** S-001
> **Editable files:** `["{{implementation path}}", "{{disposable soft-test path}}"]`
> **Locked test files:** `["{{firm test path}}", "{{behavioral safety-test path}}"]`
> **Observable invariants:** {{behavior that cleanup must preserve}}
> **Baseline command:** `{{targeted tests proving completed implementation is green}}`
> **Final command:** `{{targeted plus broader tests/static checks}}`

### Tests

- [ ] Write firm-seam test for {{seam-name}} [seam: {{seam-id}}, firmness: firm]
  - Target: `{{test file path}}`
  - Assert: `{{behavioral assertion from AC}}`
  - Traces to: `[SEAM-{{id}}-01]`

- [ ] Write unit tests for {{component}} [seam: internal, firmness: soft]
  - Target: `{{test file path}}`

### Implementation

- [ ] {{specific task}} — `{{target file}}`
  - {{concrete detail: what to add/change/delete. Complete enough for a cheap model to execute.}}

- [ ] {{specific task}} — `{{target file}}`

### Independent review and cleanup checkpoint

- [ ] Establish green snapshot with `implementation-checkpoint.mjs --baseline-test`
- [ ] Fresh read-only critic accepts an initial snapshot-bound review
- [ ] Apply behavior-preserving cleanup findings, or record a substantive no-change rationale
- [ ] Run declared final checks; firm and cycle-locked tests remain unchanged
- [ ] Fresh final reviewer verifies behavior preservation for the tested snapshot

---

## Section 2: {{section-name}}

> **Scope:** {{description}}
> **Checkpoint unit:** S-002
> **Editable files:** `["{{path}}"]`
> **Locked test files:** `[]`
> **Observable invariants:** {{invariants}}
> **Baseline command:** `{{command}}`
> **Final command:** `{{command}}`

### Tests

- [ ] {{test task}} [seam: {{seam-id}}, firmness: {{firm|soft}}]

### Implementation

- [ ] {{task}}

### Independent review and cleanup checkpoint

- [ ] Establish green snapshot
- [ ] Accept independent initial review
- [ ] Apply behavior-preserving cleanup or document no-change rationale
- [ ] Run final checks with locked tests unchanged
- [ ] Accept fresh final behavior-preservation review

---

<!-- Add sections as needed. The complete Checkpoint unit ID set must equal the canonical implementation-units.json declaration set. -->

---

## Final section: Documentation reconciliation

> **Triggered by:** `implement` gate approved, user confirms happy.
> **Goal:** Update all CONTEXT.md files in `manifest.context_targets` to reflect this change.

- [ ] Run `context-verify.mjs` — baseline drift report
- [ ] Update root `CONTEXT.md`: {{what changes}}
- [ ] Update `{{component}}/CONTEXT.md`: {{what changes}}
- [ ] Re-stamp provenance on all updated CONTEXT.md files (`validated-at: <HEAD sha>`)
- [ ] Verifier subagent: confirm CONTEXT claims match implemented code
- [ ] Present reconciliation summary to user
- [ ] User approves → docs gate → approved
- [ ] Run `change-archive.mjs` → zip to `.changes/archive/{{id}}.zip`
